# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Memo_SwiftUI' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Memo_SwiftUI

  target 'Memo_SwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Memo_SwiftUIUITests' do
    # Pods for testing
  end

end
